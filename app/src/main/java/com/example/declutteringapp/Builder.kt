/*
package com.example.declutteringapp

import retrofit2.Retrofit
import retrofit2.converter.gson.GsonConverterFactory

object Builder {

        private const val BASE_URL="https://mockapi.io/"
        private fun retrofit(): Retrofit =Retrofit.Builder().baseUrl(BASE_URL).
        addConverterFactory(GsonConverterFactory.create()).build()

        val questionsAPI:KeepOrTossApi= retrofit().create(KeepOrTossApi::class.java)
    }

*/
/**/
